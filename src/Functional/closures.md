# Замыкания

Язык Cheremsha поддерживает замыкания.

---


Пример замыкания:
```
(func make-adder (x)
(
    (
        la (y) (
        (+ x y)
        )
    )
)
)
```
Возвращаемое лямбда выражение захватывает аргумент x.